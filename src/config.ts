export const DEFAULT_HOST = 'localhost';
export const DEFAULT_PORT = 2333;
export const DAEMON_URL = `http://${DEFAULT_HOST}:${DEFAULT_PORT}`;

export const FROM_DATE = new Date('2019-7-1');

export const TITLE = 'NJUST ICPC Group';
